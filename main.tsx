import React from 'react';
import ReactDOM from 'react-dom/client';
import InviteBasedFlow from './src/InviteBasedFlow';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <InviteBasedFlow />
  </React.StrictMode>
);