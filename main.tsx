import React from 'react';
import ReactDOM from 'react-dom/client';
import InviteBasedFlow from './remixed-e4b45051';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <InviteBasedFlow />
  </React.StrictMode>
);